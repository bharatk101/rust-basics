//  Array's are fixed size elements of same data type

pub fn run() {
    let numbers: [i32; 5] = [1, 2, 3, 4, 5]; // data type and the size

    println!("{:?}", numbers);

    //  get single value
    println!("{}", numbers[0]);
    
}
